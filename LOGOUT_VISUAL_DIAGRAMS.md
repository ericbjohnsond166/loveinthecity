# 🔐 LOGOUT FLOW - Visual Architecture & Diagrams

## Component Hierarchy Diagram

```
┌────────────────────────────────────────────────────────────┐
│ App.tsx (Root Component)                                   │
│  • useState: isAuthenticated (default: false)              │
│  • Provides setIsAuthenticated to children                 │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ Return: <HashRouter><AppContent/></HashRouter>
                       │
┌──────────────────────▼─────────────────────────────────────┐
│ React Router Context (HashRouter)                          │
│  • Provides Router context                                 │
│  • Enables useNavigate() hook                              │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ Children: AppContent
                       │
┌──────────────────────▼─────────────────────────────────────┐
│ AppContent (Functional Component)                          │
│  • Props: { isAuthenticated, setIsAuthenticated }          │
│  • Has: useNavigate hook ✓ (Inside Router context)        │
│  • State: [mounted, setMounted]                            │
│  • Effects:                                                │
│    ├─ Effect #1: Check session + set mounted=true         │
│    └─ Effect #2: Watch isAuthenticated, navigate if false │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ Return: <Layout><Routes>...</Routes></Layout>
                       │
┌──────────────────────▼─────────────────────────────────────┐
│ Layout (Conditional BottomNav)                             │
│  • Shows/hides nav based on location                       │
│  • useLocation to check current path                       │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       │ Children: Routes
                       │
┌──────────────────────▼─────────────────────────────────────┐
│ Routes (Conditional Page Rendering)                        │
│  • /login: show LoginPage or redirect to /                 │
│  • /: show HomePage or redirect to /login                 │
│  • /mine: show MinePage or redirect to /login              │
│  • ... (all other routes with same pattern)                │
│                                                             │
│ Each route evaluates:                                      │
│  isAuthenticated ? <Page /> : <Navigate to="/login" />    │
└──────────────────────┬─────────────────────────────────────┘
                       │
                       ├─ LoginPage (when not authenticated)
                       ├─ HomePage, CommunityPage, etc (when authenticated)
                       └─ MinePage (when authenticated)
                           └─ "Log Out" button → onClick → onLogout()
                               └─ Calls: handleLogout() function
```

---

## State Flow Diagram

```
┌─────────────────────────────────────────────────────────┐
│ INITIAL STATE                                            │
│  isAuthenticated = false                                 │
│  mounted = false                                         │
│  localStorage = empty                                    │
│  UserSees: LoginPage                                     │
└─────────────────────────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ USER ENTERS CREDENTIALS & CLICKS "SIGN IN"               │
│  LoginPage.handleLogin() executes                        │
└─────────────────────────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ HANDLELOGIN() EXECUTES                                   │
│  1. localStorage['funloves_token'] = 'mock_jwt_token'   │
│  2. storage.set('userSession', {...})                   │
│  3. Calls: onLogin() (from parent)                       │
│  4. App.handleLogin() sets isAuthenticated = true        │
└─────────────────────────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ REACT RE-RENDERS                                         │
│  isAuthenticated: false → true                           │
│  Effect #2 dependency changes [true, navigate, true]     │
│  Effect #2 condition: mounted && !isAuthenticated        │
│                        true && false = FALSE             │
│  Effect #2: DOES NOT RUN                                │
│  Routes evaluate: show HomePage ✓                        │
│  Bottom nav visible: YES ✓                               │
│  UserSees: HomePage                                      │
└─────────────────────────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ USER NAVIGATES TO /mine                                  │
│  UserSees: MinePage with settings and logout button      │
└─────────────────────────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ USER CLICKS "LOG OUT" BUTTON                             │
│  Button onClick: console.log('Log Out button clicked')   │
│                  onLogout() → handleLogout()             │
└─────────────────────────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ HANDLELOGOUT() EXECUTES                                  │
│  1. localStorage.removeItem('funloves_token')           │
│  2. storage.remove('userSession')                        │
│  3. storage.clearUserProfile()                           │
│  4. storage.clear()                                      │
│  5. setIsAuthenticated(false) ← KEY STATE CHANGE         │
└─────────────────────────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ REACT SCHEDULES RE-RENDER                                │
│  isAuthenticated: true → false (state change)            │
│  Dependencies for Effect #2: [false, navigate, true]     │
└─────────────────────────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ EFFECT #2 RUNS                                           │
│  Condition: mounted && !isAuthenticated                  │
│             true && true = TRUE ← Condition IS TRUE!     │
│  Action: navigate('/login', { replace: true })          │
│  Result: Browser history replaced (no going back)        │
└─────────────────────────────────────────────────────────┘
                       ▼
┌─────────────────────────────────────────────────────────┐
│ BROWSER NAVIGATES                                        │
│  URL changes: #/mine → #/login                           │
│  Routes re-evaluate with isAuthenticated = false         │
│  /login route: false ? <Navigate/> : <LoginPage/>        │
│  Shows: LoginPage ✓                                      │
│  UserSees: LoginPage                                     │
└─────────────────────────────────────────────────────────┘
```

---

## Logout Execution Timeline

```
Time    Event                                   State Change
────────────────────────────────────────────────────────────
T=0ms   User clicks "Log Out" button           
        └─ console.log('Log Out button clicked')
        
T=1ms   handleLogout() starts
        ├─ console.log('🚀 LOGOUT INITIATED')
        ├─ localStorage.removeItem('funloves_token')
        └─ storage instance obtained

T=2ms   Step 1: Remove userSession
        ├─ storage.remove('userSession')
        ├─ console.log('Step 1️⃣ Removing userSession...')
        ├─ console.log('🗑️ Storage item removed: userSession')
        └─ localStorage['loveinthecity_userSession'] GONE

T=3ms   Step 2: Clear userProfile  
        ├─ storage.clearUserProfile()
        ├─ console.log('Step 2️⃣ Clearing userProfile...')
        ├─ console.log('👤 User profile cleared')
        └─ localStorage['loveinthecity_userProfile'] GONE

T=4ms   Step 3: Clear all storage
        ├─ storage.clear()
        ├─ console.log('Step 3️⃣ Clearing all storage...')
        ├─ console.log('🧹 All storage cleared')
        └─ ALL 'loveinthecity_*' keys GONE

T=5ms   Step 4: Update state
        ├─ console.log('Step 4️⃣ Setting isAuthenticated to false...')
        ├─ setIsAuthenticated(false) ← STATE CHANGE
        ├─ React queues re-render
        └─ console.log('✅ LOGOUT STATE CHANGE COMPLETE...')

T=6ms   React re-renders component
        ├─ isAuthenticated dependency changes
        ├─ Effect #2 dependencies: [false, navigate, true]
        └─ Effect #2 condition evaluates to TRUE

T=7ms   Effect #2 executes
        ├─ console.log('🔴 isAuthenticated is FALSE...')
        ├─ navigate('/login', { replace: true })
        └─ Browser history entry replaced

T=8ms   Browser navigation begins
        ├─ URL: #/mine → #/login
        ├─ Routes re-evaluate
        └─ Route /login renders: <LoginPage/>

T=9ms   LoginPage renders
        ├─ Login form visible
        ├─ Bottom nav hidden
        └─ User can see "Phone" and "Password" fields

TOTAL TIME: ~9 milliseconds (nearly instantaneous)
```

---

## Storage Clearing Diagram

```
BEFORE LOGOUT:
┌──────────────────────────────────────────┐
│ localStorage                              │
├──────────────────────────────────────────┤
│ funloves_token: 'mock_jwt_token'         │
│ loveinthecity_userSession: {...}         │
│ loveinthecity_userProfile: {...}         │
│ loveinthecity_otherData: {...}           │
└──────────────────────────────────────────┘

    ▼ Step 1
┌──────────────────────────────────────────┐
│ localStorage                              │
├──────────────────────────────────────────┤
│ ✓ funloves_token removed                 │
│ ✓ loveinthecity_userSession: {...}       │
│ ✓ loveinthecity_userProfile: {...}       │
│ ✓ loveinthecity_otherData: {...}         │
└──────────────────────────────────────────┘

    ▼ Step 2
┌──────────────────────────────────────────┐
│ localStorage                              │
├──────────────────────────────────────────┤
│ ✓ loveinthecity_userSession removed      │
│ ✓ loveinthecity_userProfile: {...}       │
│ ✓ loveinthecity_otherData: {...}         │
└──────────────────────────────────────────┘

    ▼ Step 3
┌──────────────────────────────────────────┐
│ localStorage                              │
├──────────────────────────────────────────┤
│ ✓ loveinthecity_userProfile removed      │
│ ✓ loveinthecity_otherData: {...}         │
└──────────────────────────────────────────┘

    ▼ Step 4
┌──────────────────────────────────────────┐
│ localStorage                              │
├──────────────────────────────────────────┤
│ (All 'loveinthecity_*' keys removed)     │
│ COMPLETELY EMPTY                          │
└──────────────────────────────────────────┘

VERIFICATION:
Object.keys(localStorage)
  .filter(k => k.startsWith('loveinthecity_'))
  .length === 0 ✓
```

---

## UseEffect Timing Diagram

```
Component Mount
    │
    ├─────────────────────────────────────────────────────┐
    │                                                      │
    ▼ IMMEDIATELY                                  ▼ IMMEDIATELY
┌──────────────────────┐                    ┌──────────────────────┐
│ Effect #1 Runs       │                    │ Effect #2 Runs       │
├──────────────────────┤                    ├──────────────────────┤
│ Check localStorage   │                    │ Condition:           │
│ for userSession      │ CONTINUES...       │ mounted && !auth      │
│                      │                    │ = false && false      │
│ set mounted = true   │                    │ = FALSE              │
│                      │                    │ → DOES NOT RUN       │
│ Dependencies: []     │                    │                      │
│ Cleanup: none        │                    │ Dependencies:        │
└──────────────────────┘                    │ [false, nav, false]  │
                                            │ Cleanup: none        │
                                            └──────────────────────┘
    │
    ▼ AFTER EFFECTS
Component fully mounted with:
    • mounted = true
    • isAuthenticated = true (if session found) or false (default)

────────────────────────────────────────────────────────────────

User clicks logout → setIsAuthenticated(false)
    │
    ├─────────────────────────────────────────────────────┐
    │                                                      │
    ▼ RE-RENDER                                    ▼ RE-RENDER
┌──────────────────────┐                    ┌──────────────────────┐
│ Effect #1 Does NOT   │                    │ Effect #2 RUNS       │
│ Run Again            │                    │ (dependency changed) │
├──────────────────────┤                    ├──────────────────────┤
│ Dependencies: []     │                    │ Condition:           │
│ (empty, no change)   │                    │ mounted && !auth      │
│                      │                    │ = true && true       │
│                      │                    │ = TRUE ✓             │
└──────────────────────┘                    │                      │
                                            │ Action:              │
                                            │ navigate('/login',   │
                                            │  { replace: true })  │
                                            │                      │
                                            │ Dependencies:        │
                                            │ [false, nav, true]   │
                                            └──────────────────────┘
    │
    ▼ IMMEDIATELY AFTER EFFECT #2
Browser navigates to #/login
Routes re-render with isAuthenticated = false
LoginPage displays
```

---

## Route Guard Diagram

```
Every Protected Route follows this pattern:

┌────────────────────────────────────────────────┐
│ <Route path="/page" element={                  │
│   isAuthenticated ? <Page /> : <Navigate />    │
│ } />                                           │
└────────────────────────────────────────────────┘

When isAuthenticated = true:
┌────────────────────────────────────────────────┐
│ true ? <Page /> : <Navigate />                 │
│      ▼                                          │
│  Result: <Page /> RENDERS                      │
│  User sees: Full page with content             │
└────────────────────────────────────────────────┘

When isAuthenticated = false:
┌────────────────────────────────────────────────┐
│ false ? <Page /> : <Navigate />                │
│       ▼                                         │
│  Result: <Navigate to="/login" replace />      │
│  Browser: Redirects to /login                  │
│  Routes re-eval: /login shows LoginPage        │
│  User sees: Login form                         │
└────────────────────────────────────────────────┘

For /login route specifically:
┌────────────────────────────────────────────────┐
│ <Route path="/login" element={                 │
│   isAuthenticated ? <Navigate to="/" /> :      │
│   <LoginPage />                                │
│ } />                                           │
└────────────────────────────────────────────────┘

When isAuthenticated = true (logged in):
  true ? <Navigate /> : <LoginPage />
       ▼
  Go to home (can't manually access /login)

When isAuthenticated = false (logged out):
  false ? <Navigate /> : <LoginPage />
        ▼
  Show LoginPage ✓
```

---

## Data Flow Summary

```
┌─────────────────────────────────────────────────────────┐
│ User Action: Click Logout Button                        │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│ Event Handler: MinePage Button Click                    │
│ └─ Calls: onLogout() prop                              │
└─────────────────────┬───────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│ Function: App.handleLogout()                            │
│ ├─ Clear localStorage                                   │
│ ├─ Clear StorageManager cache                           │
│ └─ Update React state: setIsAuthenticated(false) ◄──┐   │
└─────────────────────┬───────────────────────────────┼───┘
                      │                               │
                      ▼                               │
┌─────────────────────────────────────────────────────┼───┐
│ React State Update                                   │   │
│ isAuthenticated: true → false                        │   │
└─────────────────────┬─────────────────────────────────┘   │
                      │                                     │
                      ▼                                     │
┌─────────────────────────────────────────────────────────┐│
│ React Re-render Scheduled                              ││
│ Effect dependencies: [isAuthenticated, ...]             ││
└─────────────────────┬─────────────────────────────────┬──┘
                      │                                │
                      ▼                                │
┌─────────────────────────────────────────────────────┴──┐
│ Effect #2 Triggered                                    │
│ Condition: mounted && !isAuthenticated = TRUE          │
│ └─ Calls: navigate('/login', { replace: true })       │
└─────────────────────┬────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│ React Router Navigation                                 │
│ ├─ Update URL: #/mine → #/login                       │
│ ├─ Replace history (don't push)                        │
│ └─ Trigger route re-evaluation                         │
└─────────────────────┬────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│ Route Evaluation with isAuthenticated = false           │
│ ├─ /login: false ? <Navigate/> : <LoginPage/> → SHOW  │
│ ├─ /mine: false ? <MinePage/> : <Navigate/> → SKIP    │
│ └─ Others: All show <Navigate to="/login"/>            │
└─────────────────────┬────────────────────────────────────┘
                      │
                      ▼
┌─────────────────────────────────────────────────────────┐
│ UI Update                                               │
│ ├─ LoginPage renders                                    │
│ ├─ Bottom nav hidden                                    │
│ ├─ Login form visible                                   │
│ └─ User can interact (enter credentials)               │
└─────────────────────────────────────────────────────────┘
```

---

## Key Points Summary

```
┌─────────────────────────────────────────────────────────┐
│ 1. ARCHITECTURE                                         │
│   ✓ AppContent inside HashRouter (useNavigate works)   │
│   ✓ Props passed from App to AppContent                │
│   ✓ State in parent, effects in child                  │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 2. STATE MANAGEMENT                                    │
│   ✓ isAuthenticated drives everything                  │
│   ✓ setIsAuthenticated is the only action              │
│   ✓ All side effects triggered by state changes        │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 3. NAVIGATION                                          │
│   ✓ useEffect #2 handles logout navigation             │
│   ✓ No setTimeout, no race conditions                  │
│   ✓ { replace: true } prevents going back              │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 4. STORAGE CLEANUP                                     │
│   ✓ 4-step clearing process                           │
│   ✓ Prevents orphaned data                            │
│   ✓ Complete wipe before navigation                    │
└─────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────┐
│ 5. ROUTE GUARDS                                        │
│   ✓ Every protected route checks isAuthenticated       │
│   ✓ Conditional ternary operators                      │
│   ✓ Automatic redirection if not authenticated         │
└─────────────────────────────────────────────────────────┘
```

